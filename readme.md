# Venv selector for neovim

This currently works only with `pyright` and simple venv.

## usage:

Lazy:

```lua
{
    "ishay320/venv.nvim",
    opts = {}
}
```

Call `:Venv` and it will give you list of python option to switch to.
